import type {FeedbackResult,Item} from '../api/types';import SpeakButton from './SpeakButton';import {t} from '../i18n';import {api} from '../api/client';import {notify} from '../lib/tts';
export default function Feedback({result,item,hinted}:{result:FeedbackResult;item:Item;hinted:boolean}){return <div className={'feedback '+(result.result==='correct'?'success':'incorrect')} role="status"><div className="row between"><h3>{result.feedback==='accents'?t('accent'):result.result==='correct'?t('correct'):t('wrong')}</h3>{hinted&&<span className="badge">{t('hintUsed')}</span>}</div>{result.feedback==='near_miss'&&<p>{t('near')}</p>}<span className="muted">{t('reveal')}</span><div className="answer-line">{result.correct_answer}<SpeakButton text={result.correct_answer} lang={item.direction==='en2es'?'es':'en'}/></div><p>{item.payload.giveup.explanation_en}</p>{item.payload.giveup.regional_notes.map((n,i)=><span className="badge" key={i} title={n.note}>{n.region}: {n.es}</span>)}<button className="text-btn" onClick={async()=>{const note=prompt(t('reportPrompt'));if(note)try{await api('/content_issues','POST',{item_id:item.id,note});notify(t('reported'))}catch{notify(t('error'))}}}>{t('report')}</button></div>}
