import {useEffect,useState} from 'react';import {Clock3} from 'lucide-react';import {useTimer} from '../store/timer';import {duration} from '../lib/time';export default function QuestionTimer(){const [ms,set]=useState(0);useEffect(()=>{const id=setInterval(()=>set(useTimer.getState().read()),200);return()=>clearInterval(id)},[]);return <span className="timer"><Clock3 size={15}/>{duration(ms)}</span>}
