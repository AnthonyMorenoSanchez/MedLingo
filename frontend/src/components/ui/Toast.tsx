import {useState,useEffect} from 'react';export default function Toast(){const [message,setMessage]=useState('');useEffect(()=>{let timer:ReturnType<typeof setTimeout>;const show=(e:Event)=>{setMessage((e as CustomEvent).detail);clearTimeout(timer);timer=setTimeout(()=>setMessage(''),7000)};window.addEventListener('toast',show);return()=>{window.removeEventListener('toast',show);clearTimeout(timer)}},[]);return message?<div className="toast" role="status">{message}</div>:null}
