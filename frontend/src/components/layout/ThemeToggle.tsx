import {Moon,Sun} from 'lucide-react';import {useProfile} from '../../store/profile';import {t} from '../../i18n';export default function ThemeToggle(){const {settings,save}=useProfile();return <button className="icon-btn" aria-label={t('toggleTheme')} onClick={()=>save({theme:settings.theme==='dark'?'light':'dark'})}>{settings.theme==='dark'?<Sun size={18}/>:<Moon size={18}/>}</button>}
