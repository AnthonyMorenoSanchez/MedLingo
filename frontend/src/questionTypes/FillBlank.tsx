import {useState} from 'react';import type {QuestionProps} from '../api/types';import {t} from '../i18n';export default function FillBlank({item,onSubmit,disabled}:QuestionProps){const [value,set]=useState('');return <form onSubmit={e=>{e.preventDefault();onSubmit(value)}}>{item.kind==='fill_blank'&&<p className="cloze-line">{item.payload.sentence_masked}</p>}<input className="answer-input" autoFocus aria-label={t('typeAnswer')} placeholder={t('typeAnswer')} value={value} onChange={e=>set(e.target.value)} disabled={disabled} autoComplete="off"/><button className="btn check" disabled={disabled||!value.trim()}>{t('check')}</button></form>}
