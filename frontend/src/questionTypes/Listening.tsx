import {useEffect} from 'react';import type {QuestionProps} from '../api/types';import {speak} from '../lib/tts';import {t} from '../i18n';import FillBlank from './FillBlank';export default function Listening(props:QuestionProps){useEffect(()=>{void speak(props.item.payload.audio_text,props.item.payload.audio_lang)},[props.item.id]);return <><button className="audio-orb" aria-label={t('replay')} onClick={()=>speak(props.item.payload.audio_text,props.item.payload.audio_lang)}>♫</button><p className="muted center">{t('listening')}</p><FillBlank {...props}/></>}
