import type {WidgetProps} from '../types';import {Link} from 'react-router-dom';import {t} from '../../i18n';export default function LeechTable({data}:WidgetProps){return <><div className="row between"><h3>{t('leechTab')}</h3><Link to="/review" className="text-link">{t('practiceThese')} →</Link></div>{data.leech.length?data.leech.slice(0,5).map(i=><div className="table-item" key={i.id}><span>{i.payload.giveup.answer_display}</span><small>{i.correct_count} / {i.wrong_count}</small></div>):<p className="muted">{t('emptyReview')}</p>}<Link className="text-btn" to="/review">{t('saveBank')}</Link></>}
