import {LineChart,Line,XAxis,YAxis,ResponsiveContainer,Tooltip} from 'recharts';import type {WidgetProps} from '../types';import {t} from '../../i18n';export default function WrongBankTrend({data}:WidgetProps){return <><h3>{t('wrongTrend')}</h3><ResponsiveContainer width="100%" height={220}><LineChart data={data.trend}><XAxis dataKey="day" tick={{fontSize:10}}/><YAxis allowDecimals={false}/><Tooltip/><Line type="monotone" dataKey="count" stroke="#B56783" strokeWidth={2}/></LineChart></ResponsiveContainer></>}
