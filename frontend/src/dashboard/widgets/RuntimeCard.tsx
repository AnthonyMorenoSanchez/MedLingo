import type {WidgetProps} from '../types';import {t} from '../../i18n';import {duration} from '../../lib/time';export default function RuntimeCard({data}:WidgetProps){return <><h3>{t('runtime')}</h3><div className="runtime-metrics">{[['uptime',duration(data.runtime.current_uptime_s*1000)],['lifetime',duration(data.runtime.total_runtime_s*1000)],['restarts',data.runtime.restarts]].map(([k,v])=><div key={k}><span>{t(k as 'uptime')}</span><strong>{v}</strong></div>)}</div></>}
