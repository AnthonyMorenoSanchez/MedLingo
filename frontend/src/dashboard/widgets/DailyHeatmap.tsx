import type {WidgetProps} from '../types';import {localDay} from '../../lib/time';import {t} from '../../i18n';export default function DailyHeatmap({data}:WidgetProps){const days=Array.from({length:112},(_,i)=>{const d=new Date(localDay()+'T12:00:00');d.setDate(d.getDate()-111+i);const day=`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`;return {day,minutes:(data.time.daily.find(x=>x.day===day)?.study_ms||0)/60000}});return <><h3>{t('dailyActivity')}</h3><div className="heatmap">{days.map(d=><div key={d.day} title={d.day+': '+d.minutes.toFixed(1)+' min'} style={{background:d.minutes?`rgba(15,157,138,${Math.min(.95,.25+d.minutes/20)})`:'var(--border)'}}/>)}</div><p className="muted">{t('less')} ▫ ▪ {t('more')}</p></>}
