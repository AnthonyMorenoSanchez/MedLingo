import {describe,it,expect} from 'vitest';import cases from '../../../tests/shared/normalize_cases.json';import {normalize} from '../../src/lib/normalize';describe('normalization',()=>{it.each(cases)('%s normalizes to %s',(a,b)=>expect(normalize(a)).toBe(b));it('folds accents and preserves required articles',()=>{expect(normalize('riñón',false,true)).toBe('rinon');expect(normalize('la mano',true)).toBe('la mano')})});
